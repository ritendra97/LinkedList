//Remove duplicate element from sorted Linked List 
Node prv=head;
		while(prv!=null)
		{
			Node next=prv;
			while(next!=null && next.data==prv.data)
			{
				next=next.next;
			}
			prv.next=next;
			prv=prv.next;
		}
